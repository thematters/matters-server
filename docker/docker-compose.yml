version: '3'

services:
  app:
    image: matters/node:latest
    build:
      context: ../
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/var/app
      - node_modules:/var/app/node_modules
    ports:
      - "4000:4000"
    restart: always

volumes:
  node_modules:
