export enum USER_ACTION {
  follow = 'follow',
  block = 'block',
  subscribe = 'subscribe',
  upVote = 'up_vote',
  downVote = 'down_vote',
}

export enum TAG_ACTION {
  follow = 'follow',
  pin = 'pin',
}

export enum CIRCLE_ACTION {
  follow = 'follow',
  join = 'join',
}
