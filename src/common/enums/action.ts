export const USER_ACTION = {
  follow: 'follow',
  block: 'block',
  subscribe: 'subscribe',
  upVote: 'up_vote',
  downVote: 'down_vote',
}

export const TAG_ACTION = {
  follow: 'follow',
}

export enum CIRCLE_ACTION {
  follow = 'follow',
  join = 'join',
}
